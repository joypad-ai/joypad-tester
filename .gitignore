.DS_Store

# Private local-only planning / roadmap memory. Never commit.
.dev/

3do/.portfolio_os/
3do/.wine-home/
nuon/.nuon-sdk/
nuon/build/
n64/build/
n64/joypad-tester.z64

# Locally-collected build artifacts (collect.sh)
releases/

# UNFLoader USB-flash tool cache (n64/buildtools/flash.sh)
n64/buildtools/.unfloader/
